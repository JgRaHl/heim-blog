.rown
  .span8
    - blog.articles.each do |article|
      %article
        %h1
          %a{href: article.url}= article.title
        %time= article.date.strftime('%b %e %Y')

        =article.body
  .span4
    - blog.articles.each do |article|
      %h2
        %a{href: article.url}= article.title
      %author iblue
      on
      %time= article.date.strftime('%b %e %Y')

